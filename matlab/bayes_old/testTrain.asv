% testTrain

classifier = struct;
classifier.dists = cellstr('Normal','Normal','Normal');

sampling = -3:1e-3:+3;
ntrain = length(sampling);

f = zeros(ntrain,3);
c = zeros(ntrain,1);
w = zeros(ntrain,3);
for k = 1:ntrain
    
    % features
    f(k,1) = sampling(k);
    f(